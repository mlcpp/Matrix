name: Windows

on:
  push:
    branches:
    - main

  pull_request:
    branches:
    - main


jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v2
      
      - name: Add clang path to $PATH env
        shell: bash
        run: echo "PATH=$PATH:C:\msys64\mingw64\bin\" >> $GITHUB_ENV
      
      - name: Passes clang-format
        shell: bash
        run: echo "PATH=$PATH:C:\msys64\mingw64\bin\" >> $GITHUB_ENV && clang-format -i examples/*.cpp && clang-format -i tests/*.cpp && clang-format -i tests/*.hpp && clang-format -i benchmarks/*.cpp && git diff-index --quiet HEAD
      
      - name: Run CMake setup
        shell: bash
        working-directory: ./
        run: mkdir build/ && cd build/ && cmake ../
      
      - name: Build examples
        shell: bash
        working-directory: ./build
        run: make examples
      
      - name: Build tests
        shell: bash
        working-directory: ./build
        run: make tests  

      - name: Build benchmarks
        shell: bash
        working-directory: ./build
        run: make benchmarks

      - name: Run tests
        shell: bash
        working-directory: ./
        run: ./build/tests/test